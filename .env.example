# Copy to .env.production (or set in your hosting provider's build settings).
# The deployed backend's URL. Leave unset for local dev — Vite proxies
# '/api' to localhost:4000 automatically.
VITE_API_URL=https://your-backend.onrender.com
